2 - Gap Algorithms
    2.1 - Fix weird inaccuracy with new approach (related to when the personal best ghost teleports back to the checkpoint on respawn but the gap looks in the wrong checkpoint due to the time)
    2.2 - Test all gap algorithms (estimation when idx is out of range)
    2.3 - Fix the GapInfo to provide the distance

3 - Gap Related
    3.1 - Update the gap manager such that it is more functional (passing in more values to the functions)
    3.2 - as above + implement settings and stuff
    3.3 - Add a GetCP and GetLap function to the GhostInfo class instead of having loads of different functions in the gap manager
    (Add IsFinished too as a function)
    (this will be helpful to allow multiplayer support such as COTD)

4 - Re-implement save

5 - Bug Fixes
    PRIORITY - Player Data has failed and now gaps don't work (could be related to the fact I was using ghosts at the same time)
    (Probably related to when the player already has a PB so the PB ghost is named the same as the local player name so you get the ghost and not the player)

    5.1 - Respawns are still very broken (ghost may pass a checkpoint and be -4 to a ghost that has standing respawned but you are +1 because ghost has not respawned yet and you are behind. This causes giant respawn gaps)
    5.2 - Don't cache the personal best ghost because it causes issues

7 - Settings
    7.1 - Add settings for anything that needs a settings and does not have it
        - cacheSearchThreshold
        - everything gap related

    7.4 - Make settings functional
        - maxCacheEntries where ever they are created
        - cacheSearchThreshold needs to be taken into account

    7.5 - Add protection against null values when loading (consider implementing a GetOr function for JSON)
    7.6 - Add reset (just delete the current config) and importing functionalities (just copy the file into the place and rename)

8 - UI
    8.1 - Consider changing the Normal UI to show the gap of the ghost to you rather that you to the ghost (just multiply by -1)
    8.2 - Make all UI sizes relative (font, rounding, thickness)
    8.3 - Fix corner rounding

9 - Efficiency
    9.1 - Fix general efficiency and speed

10 - Finish all TODOs

11 - Future Considerations
    11.1 - implement no respawn timer
    11.2 - Consider adding a function to SampleArray that allows for the insertion of a point based on the timestamp
    11.3 - Improve display for gap rate (e.g Toggleable, seperate bar)
    11.4 - Cache interpolation (interpolate the cache so that there is less processing later)
    11.5 - Update frames between log/gap to an amount of time
    11.6 - Re-implement settings' performance options
    11.7 - Migrate Logger to use GhostData rather than whatever it does now
    11.8 - Multiplayer support (modify ghost manager to allow for collection of player data too)
    11.9 - Allow personal best to be cached but delete when PB
    11.10 - Consider re-implementing other Range checks of SampleArray such as getLap etc.